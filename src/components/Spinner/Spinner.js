import React from "react";

/*
 * A Spinner utility component
 */
const Spinner = (props) => {
  return <div className="spinner-border text-primary my-5"></div>;
};

export default Spinner;
